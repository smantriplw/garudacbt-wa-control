export const features = {
	// VervalPD Kemdikbud Feature
	vervalpdFeatures: false, // set it to 'enable' if you had access into school vervalpd site.
	// After u enabled above feature, you must set `VERVALPD_USER` and `VERVALPD_PASSWORD` in environment to make it works.
};
